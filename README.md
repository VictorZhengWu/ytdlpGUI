# ytdlpGUI

**English ([README.md](README.md)) · [中文](README.zh.md) · [日本語](README.ja.md) · [한국어](README.ko.md)**

A cross-platform graphical front-end for yt-dlp (Web / Linux / Windows) covering **every** yt-dlp command-line option, built with strict UI/logic separation, leaving the same backend API ready for future macOS / iOS / Android clients.

## Features

- **Dual-mode UI**: Simple mode by default (title bar + URL input + download queue, plain `yt-dlp <url>`); "⚙ Advanced" expands the full settings
- **All options**: every yt-dlp option (plus manually supplemented hidden ones) grouped by official categories, with localized descriptions, usage examples and tooltips; searchable
- **Mutual-exclusion governance**: 51 tri-state groups (Default/On/Off) + 30 auto-clearing pairs, metadata shared by frontend and backend, enforced server-side
- **Download management**: multi-URL job queue, live progress/speed/ETA/log (SSE), cancellation, real-time file-path reporting
- **Info lookup**: floating window with title/duration/format table — click a row to pick `-f`; per-URL results with ↑↓ navigation
- **Download history**: URL/title/path/time/status fully recorded, clearable, configurable log path
- **Directory picker**: server-side folder browser supporting LAN shares / network mounts (Windows drives / UNC paths)
- **Four UI languages**: 中文 / English / 日本語 / 한국어, fully soft-coded (`frontend/js/i18n/*.json`)
- **Presets**: save/load/delete option combos, validated on save
- **Security**: option whitelist + value validation against argument injection; listens on 127.0.0.1 by default

## Architecture

```
frontend/   Static HTML/CSS/JS (zero build, embeddable in any WebView)
backend/    FastAPI service (pure logic, independently importable)
  api/          thin HTTP routing layer
  services/     downloader / options / metadata / history / store
  data/         options_registry.json (introspected from yt_dlp.options)
docs/       SDD docs + two QA rounds (123 UI cases + 27 fixes)
scripts/    generate_registry.py / run_checks.sh / regress_examples.py
```

The option registry is generated by `scripts/generate_registry.py` via introspection of the yt-dlp source (`yt_dlp/options.py`) — flags / nargs / repeatability / exclusion relations are all data-driven. Re-run the script after upgrading yt-dlp; `--check` mode detects drift.

## Quick Start

```bash
# Linux / macOS
./run.sh                    # creates venv, installs deps, opens browser
./run.sh --lan              # LAN mode (0.0.0.0 — reachable from phones/other PCs)
```

```bat
:: Windows (from source)
run.bat
```

Manual: `python -m uvicorn backend.app.main:app --port 8765` → open http://127.0.0.1:8765
API docs (OpenAPI): http://127.0.0.1:8765/docs

## Building the Windows .exe

**Option A (recommended)**: with Python 3.10+ installed, run

```bat
build_windows.bat
```

Produces a single-file `dist\ytdlpgui.exe` — no console window, double-click to run, opens the browser UI automatically. Config/presets/history are stored in a `data\` folder next to the exe; copy the exe to distribute.

**Option B (manual)**:

```bat
python -m venv .venv
.venv\Scripts\pip install -r backend\requirements.txt pyinstaller
.venv\Scripts\pyinstaller --clean ytdlpgui.spec
```

Notes: `console=False` in `ytdlpgui.spec` (no black window); set it to `True` and rebuild to see logs while debugging. The exe bundles the current registry/frontend assets — upgrading yt-dlp requires a rebuild or running from source.

## Development quality suite

Run `scripts/run_checks.sh` after code changes or yt-dlp upgrades (all three steps must pass):

1. Registry drift check — alerts when yt-dlp's actual options differ from the registry;
2. Structural assertions on exclusion metadata and command building (non-empty sides / symmetry / multi / nargs / conflict rejection);
3. Full example regression (via build_args + yt-dlp --simulate, 96 cases).

Three-minute pre-release smoke check: page boots → "All Options" shows ≥40 tri-state groups → English UI has no Chinese leftovers.

The suite also runs automatically on GitHub Actions for every push / PR to `v2` (`.github/workflows/ci.yml`).

## Known trade-offs

- **ffmpeg is optional but recommended**: without it, high-quality videos (separate video+audio streams) cannot be merged, format conversion and subtitle embedding are unavailable. The UI shows a warning banner when ffmpeg is missing; on Windows the banner offers one-click **Auto-install** (downloads an official static build into the app's `data/bin/`, no admin rights needed). Detection reads the registry PATH live, so installing ffmpeg (winget/choco/scoop or manually) takes effect after a page refresh — no service restart
- URL input accepts http(s), `ytsearch` prefixes, and colon-less bare domains; other schemes/search prefixes are rejected (attack-surface consideration)
- `--alias` / `--replace-in-metadata` / `--print-to-file` are multi-argument options expressed as space-separated values in one input box (per-segment space limits shown in the UI)
- Pure CLI query options (`--help` / `--dump-json`, …) are hidden from the UI but still whitelisted for old presets

## Future platforms (macOS / iOS / Android)

The backend `services/` layer depends on no web-framework types and can be ported wholesale; the frontend is build-free static assets loadable via WKWebView / WebView / Android WebView, or wrappable with Tauri / Capacitor.

## Docs

- Design & tasks: `docs/design.md`, `docs/tasks.md`
- QA: `docs/qa-report-1.md` (data/logic layer), `docs/qa-ui-plan.md` + `docs/qa-ui-report.md` (123 UI cases)

## Legacy

The legacy version (tkinter, Windows-only, minimal) lives at `ytdlpGUI@7c93372`.
